# filepath: /Users/arkajyotisaha/Desktop/My-Thesis/code/requirements.txt
############################################
#  requirements.txt  –  Week‑3 (PyTorch)   #
############################################

# ─── Core data handling ───────────────────────────────────────────────────────
numpy>=1.24.0          # 1.24+ has full Apple‑silicon wheels
pandas>=1.5.0
pyarrow>=14.0.0        # parquet IO
tables>=3.9.1          # HDF5 support for pandas

# ─── Machine‑learning stack (PyTorch + sklearn) ───────────────────────────────
# Official nightly / stable wheels include MPS (Metal) support on macOS‑arm64
torch>=2.2.0           # pip will pull torch‑2.2.*‑cp310‑*.whl with mps backend
torchvision>=0.17.0    # matches torch 2.2.x ABI
scikit-learn>=1.3.0
scipy>=1.11.0          # sklearn hard‑dependency
joblib>=1.3.0          # model persistence / parallelism

# ─── High‑performance / acceleration ─────────────────────────────────────────
numba>=0.58.0          # optional JIT for custom feature engineering

# ─── Configuration & utilities ───────────────────────────────────────────────
pyyaml>=6.0
tqdm>=4.66.0

# ─── Visualisation ───────────────────────────────────────────────────────────
matplotlib>=3.8.0
seaborn>=0.13.0

# ─── File‑compression back‑ends ──────────────────────────────────────────────
python-snappy>=0.6.1   # ‘snappy’ namespace on PyPI is abandoned

 # statistics / volatility
arch>=5.5          #  for GARCH(1,1)   <— NEW
numba>=0.56.0      #  optional speed‑up for rolling ops
    #  for custom feature engineering